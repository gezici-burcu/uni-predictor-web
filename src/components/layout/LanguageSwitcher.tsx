"use client";import {useAppLanguage} from "@/src/contexts/AppLanguageContext";
export function LanguageSwitcher(){const{language,setLanguage,t}=useAppLanguage();return <div role="group" aria-label={t("header.languageSelector")} className="inline-flex h-9 rounded-xl border border-white/15 bg-white/8 p-1 backdrop-blur-xl">{(["tr","en"] as const).map(item=><button key={item} type="button" aria-pressed={language===item} onClick={()=>setLanguage(item)} className={`rounded-lg px-2.5 text-[11px] font-bold transition-all ${language===item?"bg-white text-indigo-700 shadow-sm":"text-slate-300 hover:text-white"}`}>{item.toUpperCase()}</button>)}</div>}
