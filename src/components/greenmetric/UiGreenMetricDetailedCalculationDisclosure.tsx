import {greenMetricIndicators} from "@/src/config/greenmetric.metrics";import type {UiGreenMetricCalculationResult} from "@/src/lib/calculations/ui-greenmetric";
const f=(v:number|string|string[]|null)=>v===null?"—":Array.isArray(v)?v.join(", "):typeof v==="number"?new Intl.NumberFormat("tr-TR",{maximumFractionDigits:3}).format(v):v;export function UiGreenMetricDetailedCalculationDisclosure({result}:{result:UiGreenMetricCalculationResult}){return <details className="rounded-xl border border-slate-200 bg-white"><summary className="cursor-pointer px-4 py-3 text-sm font-medium text-slate-700">Ayrıntılı hesaplama durumlarını göster</summary><div className="overflow-x-auto border-t p-4"><table className="min-w-[760px] text-left text-xs"><thead><tr>{["Kod","Gösterge","Ham Değer","Puan","Maksimum","Durum","Kaynak","Uyarı"].map(h=><th key={h} className="px-2 py-2">{h}</th>)}</tr></thead><tbody>{greenMetricIndicators.map(def=>{const r=result.indicatorResults[def.code as keyof typeof result.indicatorResults];return <tr key={def.code} className="border-t"><td className="px-2 py-2 font-semibold">{def.code}</td><td className="px-2 py-2">{def.title}</td><td className="px-2 py-2">{f(r.rawValue)}</td><td className="px-2 py-2">{f(r.score)}</td><td className="px-2 py-2">{r.maximumScore}</td><td className="px-2 py-2">{r.status}</td><td className="px-2 py-2">Geçici simülasyon girdisi</td><td className="px-2 py-2">{r.warnings.join(" ")||"—"}</td></tr>})}</tbody></table></div></details>}
