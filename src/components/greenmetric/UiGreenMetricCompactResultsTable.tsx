import {greenMetricCategories} from "@/src/config/greenmetric.categories";import type {UiGreenMetricCalculationResult} from "@/src/lib/calculations/ui-greenmetric";
const f=(v:number|null)=>v===null?"—":new Intl.NumberFormat("tr-TR",{maximumFractionDigits:1}).format(v);export function UiGreenMetricCompactResultsTable({current,scenario,changedCount}:{current:UiGreenMetricCalculationResult;scenario:UiGreenMetricCalculationResult;changedCount:number}){const categoryRows=greenMetricCategories.map(c=>({label:c.title,current:current.displayedCategoryScores[c.code],scenario:scenario.displayedCategoryScores[c.code],maximum:c.maxScore,note:"Gösterge puanlarının toplamı"}));const rows=[{label:"Genel Skor",current:current.totalScore,scenario:scenario.totalScore,maximum:10000,note:"Yedi kategori puanının toplamı"},...categoryRows];return <section className="min-w-0 overflow-hidden rounded-2xl border border-slate-200 bg-white p-4 shadow-sm"><h2 className="text-base font-semibold text-slate-950">UI GreenMetric Sonuç Tablosu</h2><div className="mt-3 overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead><tr className="border-y bg-slate-50">{["Kategori","Mevcut","Senaryo","Fark","Maksimum","Not"].map(h=><th key={h} scope="col" className="px-3 py-2">{h}</th>)}</tr></thead><tbody>{rows.map(r=>{const d=r.current===null||r.scenario===null?null:r.scenario-r.current;return <tr key={r.label} className="border-b"><th scope="row" className="px-3 py-2">{r.label}</th><td className="px-3 py-2">{f(r.current)}</td><td className="px-3 py-2">{f(r.scenario)}</td><td className="px-3 py-2">{d!==null&&d>0?"+":""}{f(d)}</td><td className="px-3 py-2">{f(r.maximum)}</td><td className="px-3 py-2 text-xs text-slate-500">{r.note}</td></tr>})}<tr><th scope="row" className="px-3 py-2">Tahmini Sıralama</th><td className="px-3 py-2">{current.estimatedRankBand??"—"}</td><td className="px-3 py-2">{changedCount===0?current.estimatedRankBand??"—":scenario.estimatedRankBand??"—"}</td><td className="px-3 py-2">—</td><td className="px-3 py-2">—</td><td className="px-3 py-2 text-xs text-slate-500">2025 tarihsel dağılımına dayalı geçici tahmin</td></tr></tbody></table></div></section>}
